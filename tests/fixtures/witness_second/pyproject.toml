[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "zealfie-witness2"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
zewitness2 = "zewitness2.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
