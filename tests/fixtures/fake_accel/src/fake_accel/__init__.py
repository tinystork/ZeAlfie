"""Synthetic accelerated distribution for M1-2I tests.

Deliberately not a real GPU framework: ZeAlfie never selects a concrete
accelerated backend in production code, and tests must not either.
"""

__version__ = "1.0.0"
